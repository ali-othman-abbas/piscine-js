function max(num1, num2) {
  if (typeof num1 != 'number' || typeof num2 != 'number') {
    return
  }
  
  return (num1 > num2) ? num1 : num2
}
function max(num1, num2) {
  if (typeof num1 != 'number' || typeof num2 != 'number') {
    return
  }
  
  return (num1 < num2) ? num1 : num2
}